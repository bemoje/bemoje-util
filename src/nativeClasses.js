export const nativeClasses = new Set(
	[
		Object,
		Function,
		Boolean,
		Symbol,
		Error,
		Number,
		BigInt,
		Math,
		Date,
		String,
		RegExp,
		Array,
		Int8Array,
		Uint8Array,
		Uint8ClampedArray,
		Int16Array,
		Uint16Array,
		Int32Array,
		Uint32Array,
		Float32Array,
		Float64Array,
		BigInt64Array,
		BigUint64Array,
		Map,
		Set,
		WeakMap,
		WeakSet,
		ArrayBuffer,
		SharedArrayBuffer,
		Atomics,
		DataView,
		JSON,
		Promise,
		Reflect,
		Proxy,
		Intl,
		WebAssembly,
		WebAssembly.Module,
		WebAssembly.Instance,
		WebAssembly.Memory,
		WebAssembly.Table,
		WebAssembly.CompileError,
		WebAssembly.LinkError,
		WebAssembly.RuntimeError,
		EvalError,
		RangeError,
		ReferenceError,
		SyntaxError,
		TypeError,
		URIError,
		BigUint64Array,
		BigInt,
		BigInt64Array,
		BigUint64Array,
		Uint32Array,
		Uint16Array,
		Uint8Array,
		Uint8ClampedArray,
		Int8Array,
		Int16Array,
		Int32Array,
	]
		.filter((c) => !!c)
		.sort()
)

nativeClasses.names = new Set([...nativeClasses].map((c) => c.name))

export default nativeClasses
